{
  "FULL_PATH": "/godot/cue/F7HR8TVD",
  "ACCESS": 0,
  "GODOT": {"KIND": "container"},
  "CONTENTS": {
    "colour": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/colour",
      "DESCRIPTION": "A #RRGGBB decoration. Never the sole carrier of information.",
      "TYPE": "s",
      "ACCESS": 3,
      "VALUE": [""],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "enabled": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/enabled",
      "DESCRIPTION": "Whether the cue runs at all. A disabled cue is skipped, not deleted.",
      "TYPE": "T",
      "ACCESS": 3,
      "VALUE": [true],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "index": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/index",
      "DESCRIPTION": "Position among siblings, zero-based.",
      "TYPE": "i",
      "ACCESS": 1,
      "VALUE": [1],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "kind": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/kind",
      "DESCRIPTION": "What kind of cue this is, derived from the element rather than stored: a Group is a group and a Media is media. Deriving it is what stops a client turning one kind into another by writing a word, and what lets the grammar refuse a file attribute on a cue that plays nothing.",
      "TYPE": "s",
      "ACCESS": 1,
      "VALUE": ["memo"],
      "RANGE": [{"VALS": ["memo", "group", "media", "fade", "stop", "osc", "midi"]}],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "name": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/name",
      "DESCRIPTION": "The cue's name, as the operator reads it.",
      "TYPE": "s",
      "ACCESS": 3,
      "VALUE": ["Announce"],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "notes": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/notes",
      "DESCRIPTION": "Free text for the designer.",
      "TYPE": "s",
      "ACCESS": 3,
      "VALUE": [""],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "number": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/number",
      "DESCRIPTION": "The decimal cue number. A string, because 12.5.1 is one; mutable, renumbered during tech, and never an identity.",
      "TYPE": "s",
      "ACCESS": 3,
      "VALUE": ["2.2"],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "parent": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/parent",
      "DESCRIPTION": "The identifier of the containing list or group.",
      "TYPE": "s",
      "ACCESS": 1,
      "VALUE": ["D9FH2JKA"],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "postWait": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/postWait",
      "DESCRIPTION": "How long after completion before this cue reports done to its parent. A group is complete once every member is, its members' own post-waits included; the group's post-wait then runs on top of that.",
      "TYPE": "d",
      "ACCESS": 3,
      "VALUE": [1.5],
      "RANGE": [{"MIN": 0}],
      "UNIT": ["s"],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "preWait": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/preWait",
      "DESCRIPTION": "How long after entry before this cue starts. A group has one of its own, and it runs before its members begin their own pre-waits, so raising it defers the whole group.",
      "TYPE": "d",
      "ACCESS": 3,
      "VALUE": [0],
      "RANGE": [{"MIN": 0}],
      "UNIT": ["s"],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "prepare": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/prepare",
      "DESCRIPTION": "How far ahead this cue has been got ready, derived from its prepared run. PRD 3.12: preparation is per PARAMETER rather than per cue, so a block that is partly anticipatable says so rather than pretending. Idle is the resting state and means nothing has been prepared, which is not a failure - a MIDI cue can never be prepared and reads idle for ever. Preparing is in progress; pending is waiting for a slot somebody else holds (3.9e, in words rather than colour); partial means something in the block could not be anticipated and will happen at entry; armed means everything that could be prepared was, with nothing to verify; verified means every pre-sent value was read back equal, which is the only one of the six that says the desk agrees.",
      "TYPE": "s",
      "ACCESS": 1,
      "VALUE": ["idle"],
      "RANGE": [{"VALS": ["idle", "preparing", "pending", "partial", "armed", "verified"]}],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    },
    "role": {
      "FULL_PATH": "/godot/cue/F7HR8TVD/role",
      "DESCRIPTION": "Where this cue sits in the thing that contains it: an ordinary member, one of the cues in its group's header or footer, or one of the list's persistent cues. Derived from the element that contains it rather than stored, like `kind` and for the same reason - a client that could write it could move a cue between a group's preparation and its members without the group knowing. A cue at the top level of a list is a member. The whole vocabulary is declared here although `persistent` cannot occur until the section exists, because an enum that grows later grows under a client that has already read it.",
      "TYPE": "s",
      "ACCESS": 1,
      "VALUE": ["member"],
      "RANGE": [{"VALS": ["member", "header", "footer", "persistent"]}],
      "GODOT": {"KIND": "state", "RATE_CAP": 50, "ANTICIPATABLE": false, "PANIC": "park"}
    }
  }
}
